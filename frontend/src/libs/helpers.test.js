import { catalogHelper } from "./helpers";
describe("implements the tests", () => {
    it("shows us the right time given the right amount of seconds", () => {
        expect(true).toBe(false);
    });
});